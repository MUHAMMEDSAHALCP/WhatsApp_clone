import 'package:flutter/material.dart';

class WebLayoutView extends StatelessWidget {
  const WebLayoutView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [],
      ),
    );
  }
}
